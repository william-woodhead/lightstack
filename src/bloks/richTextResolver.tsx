import React from "react";
import { RichTextComponent, Blok } from "../model/storyblok";
import Quote from "./Quote";

export default function(component: RichTextComponent, blok: Blok) {
  switch (component) {
  }
}
